interface MilestoneCelebrationProps {
  milestone?: number;
  onClose?: () => void;
}

// Platshållare — implementeras i Sprint 2 (animationer + konfetti)
const MilestoneCelebration = (_props: MilestoneCelebrationProps) => null;

export default MilestoneCelebration;
